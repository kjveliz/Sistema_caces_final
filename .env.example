# Backend (PHP). Copiar este archivo a ".env" en la raíz del repo y completar
# según el entorno local. ".env" NO se sube a git (ver .gitignore).

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=evaluacion_caces

CORS_ALLOWED_ORIGIN=http://localhost:5173

GOOGLE_DRIVE_CREDENTIALS_PATH=api/google_drive/credenciales.json
